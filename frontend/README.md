# Ashiato Singapore MRT Frontend

React + Vite frontend.

## Setup

```bash
npm install
npm run dev
```

The dev server uses `http://127.0.0.1:5293`.

Windows scripts are also available:

```bat
start.bat
stop.bat
restart.bat
```
